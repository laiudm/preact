/** Virtual DOM Node */

//dcm - wow this is brief. The walk-thru. code at least defines a function that sets some local attributes - nodeName, attributes, children, key
//dcm - it appears that the attributes are now initialised inline in h.js

export function VNode() {}
